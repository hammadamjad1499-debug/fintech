# Specification Source

The original assignment PDF (`FinTech_AI_Assignment.pdf`) was not attached
to this build request -- only the master build prompt describing its
contents was provided. This project was built directly from that detailed
master prompt, which itself was written to mirror the PDF's requirements
section by section (financial calculations, form fields, LangChain
concepts, JSON schema, streaming, caching, dashboard layout, testing
scenarios, etc.).

If you have the original PDF, place it here as
`docs/FinTech_AI_Assignment.pdf` for your own records and to double-check
this build against it before submission.
